apply plugin: "war"

bootWar {
    mainClassName = "com.jhipster.demo.notification.NotificationApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]
}
